// config.h — WiFi credentials and OTA settings for pivox ESP32 HID.
// Copy this file to src/config.h and fill in your values.
// config.h is .gitignored — never commit real credentials.

#pragma once

// WiFi credentials
#define WIFI_SSID      "YourNetworkName"
#define WIFI_PASSWORD  "YourWiFiPassword"

// Hostname visible in mDNS and OTA tool discovery
#define OTA_HOSTNAME   "pivox-hid"

// OTA password (optional — comment out to disable password protection)
// #define OTA_PASSWORD "your-ota-password"
