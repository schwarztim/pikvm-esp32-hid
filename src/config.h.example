// config.h.example — copy to config.h and fill in your values
// src/config.h is gitignored — never commit the real credentials file
#pragma once

#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASSWORD   "your-wifi-password"
#define OTA_HOSTNAME    "pikvm-hid"          // mDNS name: pikvm-hid.local
#define OTA_PASSWORD    "your-ota-password"  // leave "" to disable OTA auth
